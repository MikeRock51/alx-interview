#!/usr/bin/python3
"""Prime Game"""


def isWinner(x, nums):
    """Returns the winner of the game"""
    def isPrime(num):
        """Returns whether or nor a number is a prime number"""
        if num <= 1:
            return false
        if num <= 
